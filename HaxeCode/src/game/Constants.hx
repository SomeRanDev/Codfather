package game;

import godot.PackedScene;
import godot.Vector3;

import GDScript as GD;

@:const final JUMP_HEIGHT = 2.0;

@:const final GWSharkHatPosition = new Vector3(-0.345, 0.227, 0.027);
@:const final GWSharkHatRotation = new Vector3(-23.9, 138.6, -22.4);

@:const final HHSharkHatPosition = new Vector3(-0.345, 0.176 - 0.05, 0.027);
@:const final HHSharkHatRotation = new Vector3(-3.1, 127.5, -0.4);

@:const final WSharkHatPosition = new Vector3(-0.345, 0.196, 0.027);
@:const final WSharkHatRotation = new Vector3(-3.1, 127.5, -0.4);

//@:const final PROJECTILE_SCENE: PackedScene = GD.preload("res://Objects/NPCs/Projectile.tscn");
